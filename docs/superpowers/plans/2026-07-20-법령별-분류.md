# 법령(과목) 중심 분류 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 문제 탐색의 1차 분류를 태그 기반 회차/심화 컬렉션에서 8개 법령 카테고리(6법령 + 교차법령 + 심화 OX·계산) 중심으로 재구성하고, `QuestionCollection`/`subjectId` 명명 혼선을 정리한다.

**Architecture:** 콘텐츠(JSON) 미수정. 각 문항의 기존 법령 태그로 카테고리를 파생하는 순수 분류 함수(`classifyCategoryId`)를 데이터 계층에 두고, `QuestionRepository`가 이를 통해 카테고리 목록·문항 세트를 만든다. 이미 collectionId 기반으로 도는 모드·이어풀기·라우팅·홈 배관을 재사용하되, category id를 담고 있던 `subjectId` Dart 식별자를 `categoryId`로 정리한다.

**Tech Stack:** Flutter, Riverpod, drift(SQLite), go_router. 클린 아키텍처(domain/data/presentation).

## Global Constraints

- **콘텐츠 불변**: `assets/content/fire-law.json`의 지문·정답·해설·태그를 수정하지 않는다. 소방시설법 이름 변형은 코드에서 정규화한다.
- **의존성**: `docs/BUILD_PLAN.md` §0 허용목록 내에서만. 새 패키지 추가 금지.
- **레이어**: `lib/domain`은 Flutter·drift import 0(순수 Dart). `presentation`은 UseCase/Provider만 호출. 저장소 로직은 `data/**`에서만.
- **디자인 시스템**: 색·간격 하드코딩 금지. `shared/theme` 토큰·`shared/widgets` 공용 컴포넌트만.
- **검증 루프**: 각 태스크 = 구현 → `flutter analyze`(이슈 0) → 관련 테스트 통과.
- **DB**: 이번 변경은 drift 테이블 **구조 변경 없음**(schemaVersion만 상향 + 데이터 정리 migration). 따라서 `build_runner` 재생성 불필요.
- **커밋**: 태스크 단위, 한국어 메시지, 끝에 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **카테고리 id 스킴**(전 태스크 공유):
  - 소방기본법 `fire-law::law:gibon` / 화재예방법 `fire-law::law:yebang` / 소방시설법 `fire-law::law:sisul` / 소방공사업법 `fire-law::law:gongsa` / 위험물안전관리법 `fire-law::law:wiheom` / 화재조사법 `fire-law::law:josa`
  - 교차법령 `fire-law::cross` / 심화 OX·계산 `fire-law::simhwa-etc` / 전체 `fire-law`
  - group: 6법령 = `법령`, 교차·심화OX = `기타`, 전체 = `전체`
  - 예상 count: gibon 46 · yebang 49 · sisul 71 · gongsa 50 · wiheom 53 · josa 7 · cross 39 · simhwa-etc 18 (= 315 + 18; 총 333)

---

### Task 1: 법령 카테고리 카탈로그 + 분류 함수 (순수 로직)

**Files:**
- Create: `lib/data/content/law_category_catalog.dart`
- Test: `test/data/law_category_catalog_test.dart`

**Interfaces:**
- Produces:
  - `class LawCategory { final String id; final String name; final String group; }`
  - `const List<LawCategory> lawCategories` (표시 순서: 6법령 → 교차법령 → 심화 OX·계산)
  - `const LawCategory allCategory` (id `fire-law`, name `전체`, group `전체`)
  - `const String kFireLawSubjectId = 'fire-law'`
  - `String classifyCategoryId(Question q)` — 문항을 정확히 1개 카테고리 id로 분류

- [ ] **Step 1: 실패하는 분류 단위 테스트 작성**

`test/data/law_category_catalog_test.dart`:

```dart
import 'dart:convert';
import 'dart:io';

import 'package:deck_119/data/content/law_category_catalog.dart';
import 'package:deck_119/domain/entities/question.dart';
import 'package:flutter_test/flutter_test.dart';

Question _q(List<String> tags) => Question(
      id: 't',
      subjectId: 'fire-law',
      type: QuestionType.mcq,
      year: 2025,
      stem: 's',
      choices: const ['a', 'b'],
      answerIndex: 0,
      explanation: 'e',
      difficulty: 'v3',
      tags: tags,
    );

void main() {
  group('classifyCategoryId', () {
    test('법령 태그 → 해당 법령 카테고리', () {
      expect(classifyCategoryId(_q(['소방기본법'])), 'fire-law::law:gibon');
      expect(classifyCategoryId(_q(['위험물안전관리법'])), 'fire-law::law:wiheom');
    });

    test('소방시설법 이름 변형 5종 → 하나로 정규화', () {
      for (final t in [
        '소방시설 설치 및 관리에 관한 법률',
        '소방시설 설치 및 관리에 관한 법',
        '소방시설 설치 및 안전관리에 관한 법',
        '소방시설의 설치 및 관리에 관한 법률',
      ]) {
        expect(classifyCategoryId(_q([t])), 'fire-law::law:sisul');
      }
    });

    test('교차법령 태그 → 교차 카테고리', () {
      expect(classifyCategoryId(_q(['교차법령'])), 'fire-law::cross');
    });

    test('법령·교차 태그 없음 → 심화 OX·계산', () {
      expect(classifyCategoryId(_q(['OX', '경계'])), 'fire-law::simhwa-etc');
    });
  });

  test('실데이터 333문항 분류 카운트가 설계와 일치', () {
    final json = jsonDecode(File('assets/content/fire-law.json').readAsStringSync())
        as Map<String, dynamic>;
    final qs = (json['questions'] as List)
        .map((e) => Question(
              id: e['id'] as String,
              subjectId: e['subjectId'] as String,
              type: QuestionType.mcq,
              year: e['year'] as int?,
              stem: e['stem'] as String,
              choices: const ['a', 'b'],
              answerIndex: 0,
              explanation: '',
              difficulty: (e['difficulty'] ?? '') as String,
              tags: ((e['tags'] as List?) ?? const []).cast<String>(),
            ))
        .toList();

    final counts = <String, int>{};
    for (final q in qs) {
      counts.update(classifyCategoryId(q), (v) => v + 1, ifAbsent: () => 1);
    }

    expect(qs.length, 333);
    expect(counts['fire-law::law:gibon'], 46);
    expect(counts['fire-law::law:yebang'], 49);
    expect(counts['fire-law::law:sisul'], 71);
    expect(counts['fire-law::law:gongsa'], 50);
    expect(counts['fire-law::law:wiheom'], 53);
    expect(counts['fire-law::law:josa'], 7);
    expect(counts['fire-law::cross'], 39);
    expect(counts['fire-law::simhwa-etc'], 18);
  });
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `flutter test test/data/law_category_catalog_test.dart`
Expected: FAIL — `law_category_catalog.dart` 없음(컴파일 에러).

- [ ] **Step 3: 카탈로그 구현**

`lib/data/content/law_category_catalog.dart`:

```dart
import '../../domain/entities/question.dart';

/// 법령(과목) 카테고리. 콘텐츠 태그로 파생한다(JSON 미수정).
class LawCategory {
  final String id;
  final String name;

  /// 목록 섹션 구분: '법령' | '기타' | '전체'.
  final String group;
  const LawCategory({required this.id, required this.name, required this.group});
}

/// 유일 과목 id(= 콘텐츠 subjectId). '전체' 카테고리 id로도 쓴다.
const String kFireLawSubjectId = 'fire-law';

const String _p = '$kFireLawSubjectId::';

// 카테고리 id 상수.
const String catGibon = '${_p}law:gibon';
const String catYebang = '${_p}law:yebang';
const String catSisul = '${_p}law:sisul';
const String catGongsa = '${_p}law:gongsa';
const String catWiheom = '${_p}law:wiheom';
const String catJosa = '${_p}law:josa';
const String catCross = '${_p}cross';
const String catSimhwaEtc = '${_p}simhwa-etc';

/// 법령·기타 카테고리(표시 순서). '전체'는 [allCategory]로 별도.
const List<LawCategory> lawCategories = [
  LawCategory(id: catGibon, name: '소방기본법', group: '법령'),
  LawCategory(id: catYebang, name: '화재예방법', group: '법령'),
  LawCategory(id: catSisul, name: '소방시설법', group: '법령'),
  LawCategory(id: catGongsa, name: '소방공사업법', group: '법령'),
  LawCategory(id: catWiheom, name: '위험물안전관리법', group: '법령'),
  LawCategory(id: catJosa, name: '화재조사법', group: '법령'),
  LawCategory(id: catCross, name: '교차법령', group: '기타'),
  LawCategory(id: catSimhwaEtc, name: '심화 OX·계산', group: '기타'),
];

/// 홈의 빠른/랜덤/오답/진척 분모 + 전체풀기용. 법령 목록과 별도.
const LawCategory allCategory =
    LawCategory(id: kFireLawSubjectId, name: '전체', group: '전체');

/// 소방시설법 태그 변형(오탈자 포함).
const Set<String> _sisulTags = {
  '소방시설 설치 및 관리에 관한 법률',
  '소방시설 설치 및 관리에 관한 법',
  '소방시설 설치 및 안전관리에 관한 법',
  '소방시설의 설치 및 관리에 관한 법률',
};

/// 문항 → 카테고리 id (정확히 1개). 우선순위: 교차 → 각 법령 → 심화 OX·계산.
String classifyCategoryId(Question q) {
  final tags = q.tags;
  if (tags.contains('교차법령')) return catCross;
  if (tags.contains('소방기본법')) return catGibon;
  if (tags.contains('화재의 예방 및 안전관리에 관한 법률')) return catYebang;
  if (tags.any(_sisulTags.contains)) return catSisul;
  if (tags.contains('소방시설공사업법')) return catGongsa;
  if (tags.contains('위험물안전관리법')) return catWiheom;
  if (tags.contains('소방의 화재조사에 관한 법률')) return catJosa;
  return catSimhwaEtc;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `flutter test test/data/law_category_catalog_test.dart`
Expected: PASS (5 테스트).

- [ ] **Step 5: analyze + 커밋**

Run: `flutter analyze` → 이슈 0.
```bash
git add lib/data/content/law_category_catalog.dart test/data/law_category_catalog_test.dart
git commit -m "feat(content): 법령 카테고리 카탈로그 + 분류 함수(classifyCategoryId)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 명명 정리 — QuestionCollection→QuestionCategory, subjectId→categoryId (동작 불변)

순수 기계적 리네임. **동작·화면·데이터 변화 없음**(리포지토리는 여전히 회차/심화 컬렉션 반환). DB 컬럼명(`Sessions.subjectId` 등)은 리네임하지 않는다(마이그레이션 리스크 회피 — Dart 식별자만 정리).

**Files (Modify):**
- `lib/domain/entities/question_collection.dart` → **파일 리네임** `lib/domain/entities/question_category.dart`, 클래스 `QuestionCollection`→`QuestionCategory`
- `lib/domain/repositories/question_repository.dart` — `getCollections()`→`getCategories()`, import·반환타입
- `lib/data/repositories/question_repository_impl.dart` — import·`getCollections`→`getCategories`, 반환타입(본문 로직은 유지)
- `lib/domain/usecases/get_question_set.dart` — `call(String subjectId, ...)`→`call(String categoryId, ...)`
- `lib/presentation/home/viewmodel/home_view_model.dart` — `collectionsProvider`→`categoriesProvider`, import, `getCollections`→`getCategories`, `resumeInfoProvider`/`subjectStatsProvider` family 파라미터명 `subjectId`→`categoryId`, `QuestionCollection`→`QuestionCategory`(recentSessionCardProvider). **`aiBankCountProvider`의 `'fire-law'`는 유지**(진짜 subject)
- `lib/presentation/subjects/view/subjects_page.dart` — import·`collectionsProvider`→`categoriesProvider`, `QuestionCollection`→`QuestionCategory`(`_CollectionGrid`/`_CollectionRow` 타입). **group 필터 로직은 이 태스크에서 변경하지 않음**
- `lib/presentation/home/view/home_page.dart` — `collectionsProvider`→`categoriesProvider`, 지역변수 `subjectId`(line ~76, 37)→`categoryId`. **`_openAiBank`의 `const subjectId = 'fire-law'`는 유지**(진짜 subject)
- `lib/presentation/home/view/set_mode_page.dart` — `subjectStatsProvider`/`resumeInfoProvider` 호출 인자는 이미 `collectionId`. 변경 최소(참조 심볼명만 맞춤)
- `lib/presentation/quiz/viewmodel/quiz_view_model.dart` — `QuizArgs = ({String categoryId, QuizMode mode, bool resume})`, 본문 `arg.subjectId`→`arg.categoryId`(save/clear/resume 포함)
- `lib/presentation/quiz/view/quiz_page.dart` — 생성자 파라미터 `subjectId`→`categoryId`, QuizArgs 구성부
- `lib/presentation/app_router.dart` — 쿼리 파라미터 `'subjectId'`→`'categoryId'`(quiz·exam 라우트 빌더 + `quizLink`/`examLink` 파라미터명 + QuizPage 생성)
- Test(Modify): `test/support/fakes.dart`(`getCollections`→`getCategories`, `QuestionCollection`→`QuestionCategory`, import), `test/domain/get_question_set_test.dart`, `test/presentation/router_test.dart`, `test/presentation/resume_flow_test.dart`, `test/presentation/exam_flow_test.dart`, `test/presentation/quick_result_test.dart`, `test/presentation/prev_flow_test.dart`, `test/presentation/tablet_responsive_test.dart`, `test/widget_test.dart` — 리네임된 심볼·쿼리파라미터 참조 갱신

**Interfaces:**
- Consumes: (Task 1 없음 — 순수 리네임)
- Produces:
  - `class QuestionCategory { final String id; final String name; final String group; final int count; }`
  - `QuestionRepository.getCategories() -> Future<List<QuestionCategory>>`
  - `GetQuestionSet.call(String categoryId, QuizMode mode)`
  - `QuizArgs = ({String categoryId, QuizMode mode, bool resume})`
  - `Routes.quizLink(String categoryId, QuizMode mode, {bool resume})`, `Routes.examLink(String categoryId)` — 쿼리 파라미터 `categoryId`
  - Provider `categoriesProvider` (`FutureProvider<List<QuestionCategory>>`)

- [ ] **Step 1: 엔티티 파일 리네임 + 클래스명 변경**

`git mv lib/domain/entities/question_collection.dart lib/domain/entities/question_category.dart` 후 클래스명을 `QuestionCategory`로:

```dart
/// 문제 카테고리(법령·교차·심화·전체). id는 풀이·세션·통계에 쓰이는 필터 키.
class QuestionCategory {
  final String id;
  final String name;

  /// 묶음 구분('법령'·'기타'·'전체') — 목록 섹션 헤더용.
  final String group;
  final int count;

  const QuestionCategory({
    required this.id,
    required this.name,
    required this.group,
    required this.count,
  });
}
```

- [ ] **Step 2: 포트·구현·유스케이스 리네임**

`question_repository.dart`: import를 `question_category.dart`로, `getCollections()`→`getCategories()`, 반환타입 `List<QuestionCategory>`.
`question_repository_impl.dart`: import·메서드명·반환타입만 교체(본문 로직 유지 — `QuestionCollection(...)` 생성자 호출을 `QuestionCategory(...)`로 치환).
`get_question_set.dart`: `Future<List<Question>> call(String categoryId, QuizMode mode)` — 본문 `getQuestions(categoryId)`.

- [ ] **Step 3: 프레젠테이션 리네임 (provider·화면·라우터·quiz)**

위 Files 목록의 presentation 파일에서 심볼·파라미터·쿼리파라미터를 일괄 치환. 진짜 subject(`'fire-law'`) 사용처(`aiBankCountProvider`, `_openAiBank`, `getSubjects`)는 **건드리지 않는다**.

- [ ] **Step 4: 테스트·fakes 리네임**

`test/support/fakes.dart`:
```dart
// import 교체
import 'package:deck_119/domain/entities/question_category.dart';
// ...
  @override
  Future<List<QuestionCategory>> getCategories() async => [
        QuestionCategory(
            id: 's1', name: '전체', group: '전체', count: questions.length),
      ];
```
나머지 테스트 파일에서 `quizLink`/`QuizPage`/쿼리파라미터 `subjectId`→`categoryId`, `getCollections`→`getCategories`, `QuestionCollection`→`QuestionCategory` 참조를 갱신.

- [ ] **Step 5: analyze + 전체 테스트 (동작 불변 회귀 확인)**

Run: `flutter analyze` → 이슈 0.
Run: `flutter test`
Expected: 기존 전체 테스트 통과(리네임 반영). 동작·화면 변화 없음.

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "refactor(naming): QuestionCollection→QuestionCategory, subjectId→categoryId 정리(동작 불변)

DB 컬럼명은 유지(마이그레이션 회피). 진짜 subject(fire-law) 사용처는 미변경.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 리포지토리를 법령 카테고리로 전환 + 과목 화면 2섹션

리포지토리 로직을 회차/심화에서 법령 카테고리로 교체하고, 과목 화면을 법령/기타 2섹션으로 렌더. 홈 '전체' 동선은 유지된다.

**Files:**
- Modify: `lib/data/repositories/question_repository_impl.dart` — `getCategories()`·`getQuestions()` 본문 재작성(Task 1 카탈로그 사용)
- Modify: `lib/presentation/subjects/view/subjects_page.dart` — group 필터 `법령`/`기타` 2섹션, 헤더·섹션 라벨 문구
- Test: `test/data/question_repository_law_test.dart` (신규)

**Interfaces:**
- Consumes (Task 1): `classifyCategoryId(Question)`, `lawCategories`, `allCategory`, `kFireLawSubjectId`
- Consumes (Task 2): `QuestionCategory`, `getCategories()`
- Produces: `getCategories()`가 9개(6법령 존재분 + 교차 + 심화OX + 전체) 반환, `getQuestions(categoryId)`가 classify 기반 필터

- [ ] **Step 1: 실패하는 리포지토리 테스트 작성**

`test/data/question_repository_law_test.dart`:

```dart
import 'package:deck_119/data/content/law_category_catalog.dart';
import 'package:deck_119/data/datasources/content_data_source.dart';
import 'package:deck_119/data/repositories/question_repository_impl.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  final repo = QuestionRepositoryImpl(ContentDataSource());

  test('getCategories: 법령 6 + 기타 2 + 전체 1, 순서·count', () async {
    final cats = await repo.getCategories();
    final laws = cats.where((c) => c.group == '법령').toList();
    final etc = cats.where((c) => c.group == '기타').toList();
    final all = cats.where((c) => c.group == '전체').toList();

    expect(laws.map((c) => c.name).toList(),
        ['소방기본법', '화재예방법', '소방시설법', '소방공사업법', '위험물안전관리법', '화재조사법']);
    expect(etc.map((c) => c.name).toList(), ['교차법령', '심화 OX·계산']);
    expect(all.single.name, '전체');
    expect(all.single.count, 333);

    final byId = {for (final c in cats) c.id: c.count};
    expect(byId[catGibon], 46);
    expect(byId[catSisul], 71);
    expect(byId[catCross], 39);
    expect(byId[catSimhwaEtc], 18);
  });

  test('getQuestions(법령): 여러 연도(원형+심화) 병합', () async {
    final qs = await repo.getQuestions(catGibon);
    expect(qs.length, 46);
    // 원형(src:eduwill-mock 있음)과 심화(없음)가 함께 포함됨.
    expect(qs.any((q) => q.tags.contains('src:eduwill-mock')), isTrue);
    expect(qs.any((q) => !q.tags.contains('src:eduwill-mock')), isTrue);
  });

  test('getQuestions(전체): 과목 전체', () async {
    final qs = await repo.getQuestions(kFireLawSubjectId);
    expect(qs.length, 333);
  });
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `flutter test test/data/question_repository_law_test.dart`
Expected: FAIL — 아직 회차 기반이라 카테고리명/카운트 불일치.

- [ ] **Step 3: 리포지토리 본문 재작성**

`question_repository_impl.dart`의 `getQuestions`·`getCategories`를 교체(회차 정규식·`_srcTag`·`_roundTag` 제거):

```dart
import '../../domain/entities/question.dart';
import '../../domain/entities/question_category.dart';
import '../../domain/entities/subject.dart';
import '../../domain/repositories/question_repository.dart';
import '../content/law_category_catalog.dart';
import '../datasources/content_data_source.dart';

class QuestionRepositoryImpl implements QuestionRepository {
  final ContentDataSource _content;
  ContentBundle? _cache;

  QuestionRepositoryImpl(this._content);

  Future<ContentBundle> _bundle() async => _cache ??= await _content.load();

  @override
  Future<List<Subject>> getSubjects() async => [(await _bundle()).subject];

  @override
  Future<List<Question>> getQuestions(String categoryId) async {
    final qs = (await _bundle()).questions;
    if (categoryId == kFireLawSubjectId) return qs.toList(); // 전체
    return qs.where((q) => classifyCategoryId(q) == categoryId).toList();
  }

  @override
  Future<List<QuestionCategory>> getCategories() async {
    final qs = (await _bundle()).questions.toList();
    final counts = <String, int>{};
    for (final q in qs) {
      counts.update(classifyCategoryId(q), (v) => v + 1, ifAbsent: () => 1);
    }
    final result = <QuestionCategory>[
      for (final def in lawCategories)
        if ((counts[def.id] ?? 0) > 0)
          QuestionCategory(
              id: def.id, name: def.name, group: def.group, count: counts[def.id]!),
      QuestionCategory(
          id: allCategory.id,
          name: allCategory.name,
          group: allCategory.group,
          count: qs.length),
    ];
    return result;
  }
}
```

- [ ] **Step 4: 리포지토리 테스트 통과 확인**

Run: `flutter test test/data/question_repository_law_test.dart`
Expected: PASS.

- [ ] **Step 5: 과목 화면 2섹션 렌더**

`subjects_page.dart`의 `data:` 빌더 내 필터·섹션을 교체:

```dart
data: (all) {
  final laws = all.where((x) => x.group == '법령').toList();
  final etc = all.where((x) => x.group == '기타').toList();
  final twoCol = context.isTablet;
  return ResponsiveBody(
    maxWidth: AppBreakpoints.gridMax,
    child: ListView(
      padding: const EdgeInsets.fromLTRB(
          AppSpacing.xl, AppSpacing.sm, AppSpacing.xl, AppSpacing.huge),
      children: [
        Padding(
          padding: const EdgeInsets.symmetric(vertical: AppSpacing.md),
          child: Text('과목',
              style: AppText.label
                  .copyWith(color: c.textTertiary, letterSpacing: 0.3)),
        ),
        Text(subjectName ?? '소방관계법규',
            style: AppText.titleScreen.copyWith(color: c.textPrimary)),
        const SizedBox(height: AppSpacing.xs),
        Text('법령별로 문제집을 선택하세요',
            style: AppText.caption.copyWith(color: c.textTertiary)),
        if (laws.isNotEmpty) ...[
          _SectionLabel('법령별'),
          _CollectionGrid(cols: laws, twoColumn: twoCol),
        ],
        if (etc.isNotEmpty) ...[
          _SectionLabel('기타'),
          _CollectionGrid(cols: etc, twoColumn: twoCol),
        ],
      ],
    ),
  );
},
```

`_CollectionRow`의 강조 조건을 group 기준으로:
```dart
final emphasize = col.group == '기타';
```

- [ ] **Step 6: analyze + 전체 테스트 + 실앱 육안**

Run: `flutter analyze` → 0.
Run: `flutter test` → 전체 통과.
실앱: 과목 탭에서 법령 6개(법령별) + 교차법령·심화 OX·계산(기타)이 뜨고, 법령 선택 → 모드 → 풀이가 동작하는지 육안 확인.

- [ ] **Step 7: 커밋**

```bash
git add -A
git commit -m "feat(subjects): 문제 분류를 법령(과목) 중심 8카테고리로 전환

getCategories/getQuestions를 classifyCategoryId 기반으로 재작성. 과목 화면을
법령별/기타 2섹션으로. 법령 선택 시 원형+심화 병합.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 이어풀기 세션 정리 마이그레이션 (schemaVersion 3→4)

기존 회차기반 세션키는 새 카테고리와 안 맞아 orphan이 된다(무해하나 최근세션 카드가 orphan을 물면 안 뜸). schemaVersion을 올려 `Sessions`를 1회 클리어한다. **테이블 구조 변경 없음 → `build_runner` 불필요.**

**Files:**
- Modify: `lib/data/datasources/local/app_database.dart` — `schemaVersion` 3→4, `onUpgrade`에 `if (from < 4)` 클리어절
- Modify: `lib/data/datasources/local/drift_session_data_source.dart` — `_key(String subjectId)` 파라미터명 `categoryId`로(코스메틱; 주석의 `$subjectId`→`$categoryId`)
- Test: `test/data/session_migration_test.dart` (신규)

**Interfaces:**
- Consumes: (없음)
- Produces: schemaVersion 4; v3→v4 업그레이드 시 `sessions` 행 전삭제

- [ ] **Step 1: 실패하는 마이그레이션 테스트 작성**

`test/data/session_migration_test.dart`:

```dart
import 'package:deck_119/data/datasources/local/app_database.dart';
import 'package:drift/drift.dart';
import 'package:drift/native.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('v3→v4 업그레이드 시 sessions 클리어', () async {
    final db = AppDatabase.forTesting(NativeDatabase.memory());
    // 구 키 형식 세션 주입.
    await db.into(db.sessions).insert(SessionsCompanion.insert(
          key: 'fire-law::2026-1회:normal',
          subjectId: 'fire-law::2026-1회',
          lastIndex: 3,
          answers: '0,1,2',
          updatedAtMs: 1,
        ));
    // v3→v4 마이그레이션 수동 실행.
    await db.customStatement('PRAGMA foreign_keys = OFF');
    final m = db.createMigrator();
    await db.migration.onUpgrade(m, 3, 4);

    final rows = await db.select(db.sessions).get();
    expect(rows, isEmpty);
    await db.close();
  });
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `flutter test test/data/session_migration_test.dart`
Expected: FAIL — schemaVersion 3, onUpgrade에 v4 클리어절 없음(sessions 남음).

- [ ] **Step 3: 마이그레이션 구현**

`app_database.dart`:
```dart
  @override
  int get schemaVersion => 4;

  @override
  MigrationStrategy get migration => MigrationStrategy(
        onCreate: (m) => m.createAll(),
        onUpgrade: (m, from, to) async {
          // v2: AI 생성 문항 적립함 추가.
          if (from < 2) await m.createTable(generatedQuestions);
          // v3: 회수 대기 요청 테이블 추가.
          if (from < 3) await m.createTable(pendingAiRequests);
          // v4: 분류를 법령 카테고리로 재편 → 구 키 세션 클리어(T20).
          if (from < 4) await customStatement('DELETE FROM sessions');
        },
      );
```

`drift_session_data_source.dart`: `_key`·주석의 파라미터명을 `categoryId`로(코스메틱).

- [ ] **Step 4: 테스트 통과 확인**

Run: `flutter test test/data/session_migration_test.dart`
Expected: PASS.

- [ ] **Step 5: analyze + 전체 테스트**

Run: `flutter analyze` → 0.
Run: `flutter test` → 전체 통과(기존 drift_session_test 포함).

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "feat(db): schemaVersion 4 — 법령 재편으로 구 이어풀기 세션 클리어

테이블 구조 변경 없음(데이터 정리 마이그레이션). build_runner 불필요.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage** (spec 섹션 → 태스크):
- §2 카테고리 8개 + 57문항 별도 → Task 1(분류)·Task 3(getCategories). ✓
- §3 명명 정리(QuestionCollection→Category, subjectId→categoryId, 진짜 subject 유지) → Task 2. ✓
- §4 카테고리 정의·순서·소방시설법 정규화·"전체" 유지 → Task 1·Task 3. ✓
- §5 데이터/도메인/프레젠테이션 변경 → Task 2·3. ✓
- §5 세션 마이그레이션(schema 3→4, 클리어), 진척/오답 전역 유지 → Task 4(진척/오답 미변경). ✓
- §7 테스트(classify 카운트·getCategories·병합·analyze) → Task 1·3 테스트. ✓
- §8 non-goal(year 삭제·다과목·콘텐츠 수정·법령별 진척) → 계획에 포함 안 함. ✓
- §9 리스크(subjectId 이중 의미) → Task 2에서 진짜 subject 유지 명시. ✓

**2. Placeholder scan:** 없음(전 코드 스텝에 실제 코드/명령·기대 결과 기재).

**3. Type consistency:** `QuestionCategory{id,name,group,count}`, `classifyCategoryId(Question)->String`, `getCategories()->Future<List<QuestionCategory>>`, `QuizArgs.categoryId`, 카테고리 id 상수(catGibon 등)·group 문자열('법령'/'기타'/'전체')이 Task 1·2·3에서 일관.

**주의(구현자):** DB 컬럼명 `subjectId`는 리네임하지 않는다(Task 2). `'fire-law'` 문자열이 (a)전체 카테고리 id이자 (b)진짜 subject id로 이중 사용되므로, AI 문제함·`getSubjects`·`aiBankCountProvider` 경로의 `'fire-law'`는 subject로서 유지한다.
